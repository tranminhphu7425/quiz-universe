{
  "name": "quiz-universe-api",
  "version": "1.0.0",
  "description": "Quiz Universe API with Spring Boot",
  "scripts": {
    "start": "mvn spring-boot:run",
    "build": "mvn clean package",
    "test": "mvn test"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}